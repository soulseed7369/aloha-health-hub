'use client';

import dynamic from 'next/dynamic';

const WebsitePackages = dynamic(() => import('@/views/WebsitePackages'), {
  ssr: false,
});

export default function WebsitePackagesClient() {
  return <WebsitePackages />;
}
