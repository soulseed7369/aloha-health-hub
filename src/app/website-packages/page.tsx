import type { Metadata } from 'next';
import { canonicalUrl, SITE_NAME } from '@/lib/siteConfig';
import WebsitePackagesClient from './WebsitePackagesClient';

export const metadata: Metadata = {
  title: `Website Packages – ${SITE_NAME}`,
  description:
    'Custom wellness websites for practitioners and centers. Professional design, client booking, online scheduling.',
  canonical: canonicalUrl('/website-packages'),
  openGraph: {
    title: `Website Packages – ${SITE_NAME}`,
    description:
      'Custom wellness websites for practitioners and centers. Professional design, client booking, online scheduling.',
    url: canonicalUrl('/website-packages'),
    type: 'website',
  },
};

export default function WebsitePackagesPage() {
  return <WebsitePackagesClient />;
}
